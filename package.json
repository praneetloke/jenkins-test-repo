{
  "scripts": {
    "test:html": "vitest run --reporter=html",
    "test:json": "vitest run --reporter=json --outputFile=./summary.json"
  },
  "dependencies": {
    "vitest": "^3.1.3"
  },
  "devDependencies": {
    "@vitest/ui": "^3.1.3"
  }
}
